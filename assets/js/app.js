document.addEventListener('DOMContentLoaded',()=>{const buttons=document.querySelectorAll('.filter'),cards=document.querySelectorAll('.project-card');buttons.forEach(button=>button.addEventListener('click',()=>{const filter=button.dataset.filter||'all';buttons.forEach(item=>item.classList.toggle('active',item===button));cards.forEach(card=>{card.hidden=!(filter==='all'||(card.dataset.category||'')===filter)})}));const form=document.querySelector('.contact-form');if(form){const send=form.querySelector('.contact-send');if(send){send.addEventListener('click',e=>{e.preventDefault();const data=new FormData(form);const product=data.get('product')||'General';const subject=`Portfolio Inquiry — ${product}`;const body=[`Name: ${data.get('name')||''}`,`Organization: ${data.get('organization')||''}`,`Email: ${data.get('email')||''}`,`Product: ${product}`,'',`${data.get('message')||''}`].join('\n');window.location.href=`mailto:contact@blackmevally.dev?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(body)}`})}}});
