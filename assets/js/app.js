document.addEventListener('DOMContentLoaded',()=>{const buttons=document.querySelectorAll('.filter'),cards=document.querySelectorAll('.project-card');buttons.forEach(button=>button.addEventListener('click',()=>{const filter=button.dataset.filter||'all';buttons.forEach(item=>item.classList.toggle('active',item===button));cards.forEach(card=>{card.hidden=!(filter==='all'||(card.dataset.category||'')===filter)})}));document.querySelectorAll('.contact-form').forEach(form=>{const send=form.querySelector('.contact-send');if(!send)return;send.addEventListener('click',e=>{e.preventDefault();if(!form.reportValidity())return;const data=new FormData(form);const product=data.get('product')||'General';const subject=`Portfolio Inquiry — ${product}`;const body=[`Name: ${data.get('name')||''}`,`Organization: ${data.get('organization')||''}`,`Email: ${data.get('email')||''}`,`Product: ${product}`,`Phone/WhatsApp: ${data.get('phone')||''}`,`Timeline: ${data.get('timeline')||''}`,'',`Requirements: ${data.get('message')||data.get('scope')||''}`].join('\n');window.location.href=`mailto:contact@blackmevally.dev?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(body)}`})})});